[Unit]
Description=GARM Ngrok Tunnel
After=multi-user.target

[Service]
Type=simple
ExecStart=/snap/bin/ngrok http --domain=${NGROK_DOMAIN} --authtoken=${NGROK_AUTH_TOKEN} --log=stdout 9997
Restart=always
RestartSec=5s

[Install]
WantedBy=multi-user.target
